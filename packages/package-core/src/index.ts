const coreFunction1 = (): string => 'Hello';

const coreFunction2 = (): string => 'World';

const coreFunction3 = (arg: string): string => `${arg}!`;

export { coreFunction1, coreFunction2, coreFunction3 };
